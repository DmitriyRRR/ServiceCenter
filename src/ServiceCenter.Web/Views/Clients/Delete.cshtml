@model ServiceCenter.Domain.Entities.Client
@{
    ViewData["Title"] = "Delete Client";
    ViewData["Breadcrumb"] = $"<a href='/Clients' class='text-decoration-none'>Clients</a> / Delete";
}

<div class="d-flex align-items-center justify-content-between mb-4">
    <h4 class="fw-semibold mb-0">Delete Client</h4>
    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Back
    </a>
</div>

<div class="card border-danger" style="max-width:520px">
    <div class="card-header bg-danger text-white fw-semibold">
        <i class="bi bi-exclamation-triangle me-2"></i>Confirm Deletion
    </div>
    <div class="card-body">
        <p>Are you sure you want to delete <strong>@Model.FirstName @Model.LastName</strong>?</p>

        @if (Model.Devices.Any() || Model.Tickets.Any())
        {
            <div class="alert alert-warning small mb-3">
                <i class="bi bi-exclamation-circle me-1"></i>
                This client has <strong>@Model.Devices.Count device(s)</strong> and
                <strong>@Model.Tickets.Count ticket(s)</strong> linked to them.
                Deleting may fail if related records exist.
            </div>
        }

        <dl class="row mb-0 small">
            <dt class="col-sm-4 text-muted">Phone</dt>
            <dd class="col-sm-8">@Model.Phone</dd>
            <dt class="col-sm-4 text-muted">Email</dt>
            <dd class="col-sm-8">@(Model.Email ?? "—")</dd>
        </dl>
    </div>
    <div class="card-footer d-flex gap-2">
        <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
            <button type="submit" class="btn btn-danger">
                <i class="bi bi-trash me-1"></i>Yes, Delete
            </button>
        </form>
        <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
    </div>
</div>
